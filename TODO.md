# TODO's

Below are ideas that we could implement.
DO NOT IMPLEMENT RIGHT NOW
 
- Ability to move an item to a different day without specifying an time-interval.
- Command line switch to only view one day instead of the whole week.
- After not using the tool for longer period, state.TimeTravelingDate should reset.
- Ability to use a project "alias", so that you could use "myfavproject" instead of "p4-8".
  - This could also solve the current quirk that project numbers can suddenly change.
